create database if not exists mentor_management_system;

use  mentor_management_system;

create table student(name varchar(255) , usn char(10) primary key,password varchar(32),phone_no char(10),email varchar(255));

