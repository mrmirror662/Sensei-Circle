create database if not exists mentor_management_system;

use database mentor_management_system;

create table student(name varchar(20) , usn char(10) primary key,password varchar(20),phone_no char(10),email varchar(20));

